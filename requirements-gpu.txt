# ============================================================
# PPT-TTS-Project - GPU 版本依赖
# 适用于使用 CosyVoice 本地 TTS 的场景
# 需要 CUDA 12.6 + Python 3.10 环境
# ============================================================

# ============================================================
# 第一部分：PyTorch (CUDA 12.6)
# 使用阿里云镜像源，与服务器 CUDA 版本匹配
# ============================================================
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.4.0
torchaudio==2.4.0

# ============================================================
# 第二部分：ONNX Runtime GPU
# 注意: Microsoft Visual Studio Feed 已需要认证
# 如果 onnxruntime-gpu 安装失败，可尝试:
# 1. 单独下载 whl: https://github.com/microsoft/onnxruntime/releases
# 2. 或使用 CPU 版本: pip install onnxruntime
# ============================================================
onnxruntime-gpu>=1.16.0

# ============================================================
# 第三部分：CosyVoice 核心依赖
# ============================================================
# 音频处理
numpy>=1.24.0,<2.0
scipy>=1.11.0
soundfile>=0.12.0
librosa==0.10.2

# 模型加载
transformers>=4.40.0
modelScope>=1.20.0

# 其他依赖
omegaconf>=2.3.0
hydra-core>=1.3.2
pydantic>=2.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
rich>=13.0.0
